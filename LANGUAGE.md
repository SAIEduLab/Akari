# あかり 1.0.0 — 使い方・言語仕様

あかりの初公開版は1.0.0です。この文書は、その使い方、日本語文型と実行意味を定める正本です。製品版と保存・実行形式番号は別に定義します。

本文は1.0.0の実装と整合させた利用者契約です。製品・言語・runtime版は1.0.0、保存・実行形式番号は3です。契約内容の確定と、GitHub Actionsを含む全受入条件の通常PASS・製品完成認定は区別します。

## はじめに：最初の作品を動かす

あかりは、日本語の定型文で画面上の部品を動かすプログラミング環境です。`Akari.html` をローカルのブラウザーで開くと、このHTMLだけで編集・実行できます。まず「▶ 動かす」を押してマスコットをクリックすると点数が増えます。「■ 止める」で編集に戻ります。道具箱の部品をクリックして追加し、画面上でドラッグして配置します。名前・大きさ・色は「せってい」で編集します。部品を選んでDeleteキーを押すと削除します。文字入力中は削除されません。

## 画面の見方

上部には、作品全体を扱う「新しく作る」「開く」「保存」「書き出し」と、実行する「▶ 動かす」「■ 止める」があります。画像や音をすぐ追加する「画像を置く」「音声を追加」と、まとめて管理する「画像・音声を管理」、利用者定義の処理を作る「動作・計算」、変数やリストを作る「データ」、問題を調べる「作品チェック」もここから開きます。

表示は「かんたん」「くわしく」「全部」から選べます。「かんたん」は主要な操作に絞り、「くわしく」「全部」では編集・デバッグ用の操作も表示します。表示を変えても作品の本文や保存内容そのものは変わりません。

道具箱では文字、ボタン、入力欄、四角、標準キャラクターなどを追加します。作品の画面で部品を選択・配置し、「作品」で登録済みの部品を選び、「せってい」で名前・位置・大きさ・色などを編集します。「動きのようす」では実行中の状態や出力を確認します。

本文欄では「動かすもの」と「できごと」を選び、その組合せに対する本文を書きます。「コード」と「ブロック」は同じ本文の二つの表示方法です。

## 自分の作品を一つ作る

最初は、部品を一つ動かす小さな作品を作ると画面とコードの対応を確認できます。

1. 道具箱の標準キャラクターから、マスコットや星などをクリックして作品の画面へ追加します。マスコットも他の標準キャラクターと同じように何体でも追加できます。
2. 追加したキャラクターを作品の画面または「作品」で選びます。必要なら「せってい」で名前や位置を変えます。
3. 本文欄の「動かすもの」でそのキャラクター、「できごと」で「クリックしたとき」を選びます。
4. コード表示なら、本文へ次の1行を書きます。

```text
10歩動く。
```

ブロック表示なら、「動き」から同じ「10歩動く」のブロックを本文へ入れます。

5. 「▶ 動かす」を押し、作品の画面でそのキャラクターをクリックします。10歩動けば、部品・できごと・本文が結び付いています。
6. 「■ 止める」で編集に戻ります。うまく動かないときは「作品チェック」で場所・行・理由を確認します。
7. 「保存」で作品ファイルを保存します。

この流れを基本に、できごとを変えたり、命令を増やしたり、変数・リスト・画像・音を加えて作品を広げます。

## 画像・音声を作品に入れる

画像を新しいキャラクターとして使うだけなら「画像を置く」、音を追加するだけなら「音声を追加」が最短です。背景・衣装・音をまとめて扱うときは「画像・音声を管理」を開きます。

「画像・音声を管理」では、用途に応じて次の操作を使います。

- 画像を新しいキャラクターとして置く：「画像をオブジェクトとして追加」
- 画像を画面全体の背景にする：「背景画像を追加」
- 画像を既存キャラクターの衣装にする：先にキャラクターを選び、「選択中へ衣装画像を追加」
- 音を使う：「音声ファイルを追加」
- 単色の背景や文字による衣装を作る：「色背景・文字衣装を追加」

画像はPNG/JPEG/WebP、音はMP3/WAVを使えます。追加した背景・衣装・音は、後述する命令から名前で利用できます。

## 保存して開き直す

「保存」は編集を続けるための `.akari.md` を作ります。あとで続けるときは「あかり」を開いてから「開く」でそのファイルを読み込みます。ブラウザー内の自動保存もありますが、この端末・ブラウザー内だけなので、残したい作品は別にファイル保存します。

完成した作品を編集画面なしで動かしたいときは「書き出し」を使います。実行に必要な仕組みと素材を含む単体HTMLが作られます。詳しい保存形式と復元・書き出しの条件は「保存・復元・書き出し」で定めます。

## 書く場所と実行の順序

「動かすもの」で画面または部品、「できごと」で実行のきっかけを選び、本文だけを書きます。一つのできごとには一つの本文を登録できます。開始時は画面、部品の登録順に処理が作られます。複数の処理は命令単位で順番に実行され、待っている処理以外は進みます。開始の本文が終わっても、停止するまでクリックなどを受け付けます。命令は通常、選んだ対象自身に作用します。

## コードとブロックで同じ本文を編集する

本文欄の「コード」「ブロック」で、選んだ「動かすもの・できごと」の本文の表示を切り替えます。「動作・計算」で作る動作・求値の下書きは、その本文欄にある同じ切替を使います。最初はコード表示で、表示「かんたん」でも切り替えられます。コードは日本語の文を直接編集する表示、ブロックは命令・値・条件を組み立てる表示です。実行結果や使える言語機能は表示方法で変わりません。

コード表示の「入力候補」と、「動きのようす」にある「出力」は、見出しを押すと開閉します。ブロック表示では、ブロックの道具箱、広い作業台、作品の画面を並べて使います。必要なときは「作業台を広く」で作品の画面をたたみ、「画面も見る」で戻せます。

切り替えるだけでは、本文の表記、句点、空行、字下げ、コメント、未保存状態、元に戻す・やり直すの履歴を変えません。切替先で編集位置に対応する文を選び、本文ごとの選択・表示位置を保ちます。文法エラーがある本文はコード表示に残し、ブロック表示への切替を保留します。文法を読める本文なら、未登録の名前や対象外命令も消さず、診断とともにブロック表示します。

道具箱のブロックをつかんで作業台へ運ぶと、つなげられる位置が先に表示されます。命令は上下につなぎ、条件分岐やくり返しは囲いの中へ命令を入れ、値・計算・条件は対応する穴へはめて組み立てます。ブロック本体をつかんで並べ替えや入れ子の変更もできます。クリックやキーボードによる追加・移動も利用できます。数・文字・名前・所属・真偽を欄から編集し、式はさらに式を入れ子にして組み立てます。データ・部品・素材・動作・求値の名前は候補から選べ、未登録の名前も保持・修正できます。

道具箱の検索と「動き」「見た目」「もし・くり返し」などの分類ボタンでブロックを探します。「候補の表示」を開くと「すべてのブロック」「つなぐ命令」「はめる値」を切り替えられます。新しく使えない候補を調べるときは「使えない候補も表示」を選びます。使えない候補を押すと理由を表示し、本文には追加しません。既に本文にある対象外の命令や式は、診断とともに表示します。

命令は上から順につながり、条件分岐や反復は囲いの中に本文を持ちます。各ブロックの「…」を開くと、「移動」「複製」「上へ」「下へ」「削除」など、そのブロックで使える操作が現れます。値や式の操作は、その欄を選ぶと現れます。ブロックの入力欄や「…」以外の本体をつかんでも組み替えられ、囲いを動かすと中身も一緒に動きます。キーボードでは Tab で欄やボタンへ移り、Enter で「…」を開いて操作を選べます。Escape でメニューや移動を取り消せます。

可変長の入力は「＋」で増やし、入力を選ぶと現れる「↑」「↓」「外す」で並べ替え・削除します。長い本文は一つの連続した作業台のまま上下左右へ移動して編集できます。余白をドラッグして作業台を動かし、「－」「＋」で拡大率を変え、「見わたす」で本文全体、「はじめへ」で先頭へ戻れます。ブロックを端へ運ぶと、その方向へ作業台が自動で動きます。組み込み求値の可変長引数を、利用者定義の引数上限32個に制限しません。

新しい条件分岐・反復には、実際の命令「何もしない」が一つ入ります。最上位の本文は空にできますが、条件分岐の各枝と反復の本文には一つ以上の実行文が必要です。最後の実行文の削除は拒否され、「何もしないへ」で置き換えるか、囲い全体を削除できます。条件分岐の「…」内にある「そうでなければ」を「あり」「なし」に変えて枝を追加・削除します。必須の式を移動するときは、移動元に置く式も選び、両方を一度に確定します。

コメント行は専用の本文欄で編集します。命令と「そうでなければ」の行末コメントを付けるには、命令の「…」から「注釈」を開き、「※」から入力します。既に注釈があるブロックでは、表示されたコメントを押して編集欄を開けます。コメント行の欄に改行を入れると、その順の複数のコメント行になります。コメントの文字、先頭・末尾の空白、空のコメント、注釈の所属は保存されます。1.0.0では値の編集は対応する原文範囲、構造編集は最小限の安全な範囲を書き換え、他の語順・空白・コメントを保持します。一行ifへ本文を追加する場合は、そのifを字下げ形式に展開します。語順を変えた命令でも、選択と値編集は横・縦・時間などの役割に対応します。

### 未確定の入力と取り消し

入力途中の空欄や「－」だけの数、日本語の変換、ドラッグの途中は、まだ本文へ反映しません。確定できる欄はフォーカス移動時や「入力を確定」で反映します。不完全な入力が残るときは、その欄と理由を示し、保存・実行・書き出し・対象や本文の変更・モード切替を保留します。欄を直すか「取消」「入力を取り消す」で元の値に戻してください。日本語IMEの変換中は、変換を確定してから操作します。

ドラッグのプレビュー中は元の本文を保ち、妥当な場所で離すと一度だけ確定します。領域外で離す、Escapeで取り消す、ポインター操作が中断する場合は、元の位置・本文・履歴を保ちます。命令を式に入れる、式を変更先の名前に入れる、自分自身の中へ移動する操作はできません。変数や求値結果の型がまだ分からないだけで、式の接続を拒否しません。型や値の範囲は実行時にも検査します。

### 元に戻す・やり直す

「元に戻す」「やり直し」と Ctrl/Cmd+Z、Ctrl/Cmd+Y または Ctrl/Cmd+Shift+Z は、コード・ブロック・定義の下書きを共通の履歴で扱います。一つのブロック編集は、本文や注釈を含め一つの操作として戻せます。内容が同じ操作、失敗した操作、取消、表示方法だけの切替は新しい編集履歴を作らず、やり直しも消しません。元に戻す前にも未確定の入力を確認します。

## 日本語の書き方

1行に1命令またはブロック開始文を書きます。人間には自然な日本語として読め、機械には意味が一意になる定型文を使います。主語・対象の省略は、選択した実行対象から一意に決まる場合に限ります。

「もし 条件なら、次のことをする」「次のことを3回くり返す」などに続く本文は、半角スペースで深く字下げします。同じブロックの文は同じ字下げ位置にそろえます。字下げを戻すとブロックが終わり、存在しない位置への戻りはエラーです。最上位は字下げしません。タブ・全角空白による字下げは禁止です。入力幅は任意ですが、「コードを整える」は1段を半角スペース2個にします。空行・コメント行は範囲を変えません。空の処理を書く場合は、本文に「何もしない」を置きます。この文は1命令を進め、状態を変更しません。

ブロック開始文の後でEnterを押すと、次の行を自動で字下げします。日本語IMEの変換中には自動字下げしません。Tabはフォーカス移動に使います。「そうでなければ、次のことをする」は、直前の同じ字下げ位置の「もし」にだけ対応します。一行形式は「もし Cなら、S」「Cならば、S」「そうでなければ、S」「でなければ、S」と書けます。Cは条件、Sは一つの非ブロック文です。

文末の「。」は省略して入力できますが、整形では付けます。反復の「繰り返す」は「くり返す」へ整えます。文字は「こんにちは」のようにかぎ括弧で囲み、1行で書きます。文字列内部の「」を表すエスケープ記法はありません。※から行末まではコメントですが、文字列内の※は文字です。演算子・括弧・数字は全角と対応する半角を使えます。文字列内の表記は整形で変更しません。

ブロック終端「ここまで」は受理しません。範囲は字下げで表します。

### 同じ意味の別の書き方

次の別表現も、標準形と同じ命令として読み取ります。「コードを整える」では表の標準形になります。文字列・【名前】・括弧内の式を、外側の助詞や句順として読み替えません。名前の登録数や現在の値で文の区切りを変えることもありません。

| 標準形 | 別表現 |
|---|---|
| もし Cなら、次のことをする | もし Cならば、次のことをする／Cなら、次のことをする／Cならば、次のことをする／もし Cなら／もし Cならば（短い形は末尾の読点を一つ付けてもよい） |
| そうでなければ、次のことをする | そうでなければ／でなければ／でなければ、次のことをする（短い形は末尾の読点を一つ付けてもよい） |
| 次のことをE回くり返す | E回くり返す／E回だけくり返す／E回、次のことをくり返す |
| 次のことをずっとくり返す | ずっとくり返す |
| Lの各要素をIとして、次のことをくり返す | Lの各要素をIとしてくり返す |
| XがYより小さい | XがY未満／YよりXが小さい |
| XがYより大きい | XがYを超える／XがYを超えている／YよりXが大きい |
| XがYと同じ／XがYと違う | XがYと等しい／XがYと異なる／XがYと等しくない |
| RをEにする | RにEを代入する／EをRに代入する |
| RにEを足す | RにEを加える／EをRに足す／EをRに加える／RをE増やす／RをEだけ増やす |
| RからEを引く | EをRから引く／RをE減らす／RをEだけ減らす |
| LにEを追加する | Lの末尾にEを追加する／EをLに追加する／EをLの末尾に追加する |
| LのN番目にEを挿入する | EをLのN番目に挿入する |
| LのN番目を削除する | LからN番目を削除する |
| Lを空にする | Lのすべての要素を削除する／Lのすべてを削除する |
| QにEを入れる | EをQに入れる |
| 右にA度回る／左にA度回る | A度右に回る／A度左に回る |
| 横X、縦Yの位置へ行く | 横X、縦Yの位置に行く／縦Y、横Xの位置へ行く／縦Y、横Xの位置に行く |
| T秒で横X、縦Yの位置へ滑る | 座標は縦Y、横Xの順でもよい。目的地を先にして「横X、縦Yの位置へT秒で滑る」とも書ける。この「へ」の直後だけは読点を一つ付けてもよい。「滑る」は「すべる」でもよい |
| FHzの音をT秒鳴らす | T秒、FHzの音を鳴らす（終わるまで待つ形も同じ順序変更ができる） |
| T秒待つ | T秒間待つ |
| Eとたずねる | Eと尋ねる／Eと聞いて待つ |
| Eと言う | Eという |

Cは真偽式、Rは変更先のデータ名、Lはリスト名、Iは反復項目名、Qは入力欄の指定です。他の記号は式です。長い・短い見出しには字下げした本文が必要です。一行形式では、条件末尾の「なら／ならば」の後ろに読点と一つの本文を書きます。「もし」を省略した「Cなら」「Cならば」（末尾読点も可）も同じ条件見出しです。elseの別形も直前の同じ字下げのifにだけ対応します。

一行ifと字下げelse、その逆も使えます。一行の本文に字下げ本文を付け足すこと、同じ行のelse、入れ子の一行if、複数命令の連結はできません。本文内で必要な読点（座標・音など）は使えます。elseは空行・コメントを挟んでも、直前の同じ字下げ位置のifにだけ結び付きます。別の実行文を挟んだelse、孤立・重複elseは拒否します。一行本文の行末コメントは、その本文の文に属します。

条件の短い接続は、「未満のあいだ／未満であるあいだ／未満になるまで」「超えているあいだ／超えるまで」「等しいあいだ／等しくなるまで」「異なるあいだ」を使えます。「あいだ」はその位置で「間」とも書けます。反復は「、次のことをくり返す」と本文を、待機は「まで待つ」を続けます。その他の組合せや複合条件は「条件（…）が成り立つあいだ／まで」を使います。

句の中身は並べ替えません。横・縦は各一回、角度の「度」、音の「Hz」、時間の「秒」、時間を先にした音の読点など、役割を示す部分が必要です。「RをEに増やす」「秒後に滑る」、滑る命令の「位置に」、裸の「聞く」などへは広げません。固定語と区別しにくい名前は【名前】、複雑な式は括弧で区切ってください。「増やす／減らす」直前の「だけ」は固定語なので、名前の一部なら名前を囲みます。不足や重複を自動補完しません。

評価は標準形と同じ役割順です。座標は横、縦、滑る命令は時間、横、縦、音は周波数、時間、比較はX、Yの順に評価します。逆順で書いても乱数の割当ては交換されません。代入は値を評価してから変更先を書き換え、加減算は変更先の現在値を確認してから差分を評価します。リスト挿入はリスト、番号と範囲、値の順に確認します。短絡・待機・回数反復の開始時一回評価・リスト反復の開始時snapshotも標準形と同じです。

## 名前・変数・リスト

作品全体または各部品のデータは「データ」で先に作ります。変数には数・文字・真偽、リストにはそれらの並びを入れます。入れ子のリストは使えません。名前は1〜32文字で、空白・括弧・制御文字・予約語などは使えません。普通の名前は「点数」のようにそのまま書けます。助詞や演算子を含む名前、数字で始まる名前、「端」「空白」など固定の語と同じ名前は【手がかり】・【端】のように囲みます。「自分の点数」「作品の【手がかり】」で所属を明示できます。読取りの優先順位は反復の項目名、局所データ、引数、個体データ、作品データです。名前の区切りは登録数によって変わりません。「データ」と入力候補で実際の書き方を確認できます。ブロックでは名前の左にある選択欄（所属）を開き、「指定なし」「【 】」「自分の」「作品の」を選んで、区切り・所属を編集します。「指定なし」のときは矢印だけを表示し、明示した区切り・所属は名前の隣に表示します。助詞を含む名前などには先に「【 】」または所属を選んでください。モード切替では名前の囲みと所属も保ちます。次の実行は初期値から始まります。

```text
点数を0にする
点数に1を足す
点数から1を引く
名前一覧に「あかり」を追加する
名前一覧の1番目を「ひかり」にする
名前一覧の1番目に「ほたる」を挿入する
名前一覧の1番目を削除する
名前一覧を空にする
```

## 条件分岐と反復

条件は真または偽になる式です。比較は「が〜以上／以下／より大きい／より小さい／と同じ／と違う」、組合せは「かつ／または」、否定は「ではない」です。「かつ」「または」は必要な側だけを評価します。複合条件や任意の真偽式を使う条件反復・待機では「条件（…）が成り立つ」という名詞＋述語の形にします。「まで」は条件が真になったら終了し、最初から真なら本文を実行しません。回数は開始時に一度だけ評価する0以上の整数です。

```text
もし 【点数】が10以上なら、次のことをする。
  「10点以上です」と言う。
そうでなければ、次のことをする。
  「10点未満です」と言う。
次のことを10回くり返す。
  【点数】に1を足す。
条件（【点数】が100より小さい）が成り立つあいだ、次のことをくり返す。
  【点数】に1を足す。
条件（「空白」キーが押されている）が成り立つまで、次のことをくり返す。
  0.1秒待つ。
条件（【点数】が100以上）が成り立つまで待つ。
```

## 短い条件文でくり返す・待つ

簡単な条件は下の短い形で書けます。反復は「、次のことをくり返す」を末尾に付けて本文を字下げします。「以上・以下・と同じ」は「であるあいだ」「になるまで」、「より小さい・大きい」は「あいだ」「小さく・大きくなるまで」とつなぎます。条件は本文の前に判定します。「待つ」の間もほかの処理は進みます。接触条件は部品上で使います。

```text
点数を0にする。
点数が3より小さいあいだ、次のことをくり返す。
  点数に1を足す。
点数と言う。

空白キーが押されるまで待つ。
空白キーが押されなくなるまで待つ。
※ 部品上の例：端に触れるまで待つ
```

## リストを一つずつ処理する

反復開始時のリストを複製し、先頭から順に一つずつ処理します。項目名は本文の中だけで使える読み取り専用の名前です。空のリストなら本文を実行しません。途中で元のリストを変更しても、その反復の順番と内容は変わりません。番号は1から数えます。範囲外の読込・更新・削除はエラー、挿入だけは末尾の次の番号も使えます。

```text
名前一覧の各要素を項目として、次のことをくり返す。
  項目と言う。
※ 内側の反復を終了する：このくり返しを終える
※ 内側の反復の次の回へ：次のくり返しへ進む
```

## 数・文字・式

数は有限の数値だけを扱い、暗黙の文字変換はしません。四則演算は＋－×÷、累乗は＾、括弧は（）。累乗は右結合で、単項符号より優先します。次に乗除、加減、比較、否定、かつ、またはの順です。数（文字）で数に、文字（値）で文字に変換します。乱数（下限、上限）は両端が整数なら両端を含む整数です。「つなぐ」は2個以上の数・文字・真偽を連結します。長さと文字位置はUnicodeのコードポイント単位です。

```text
2＋3×4
（2＋3）×4
乱数（1、10）
余り（10、3）
つなぐ（「点数：」、【点数】）
【名前一覧】の長さ
【名前一覧】の1番目
「こんにちは」の文字数
「こんにちは」の2文字目
【名前一覧】に「あかり」が含まれている
【名前一覧】で「あかり」が最初にある番号
```

## 動き・見た目・ペン

座標は画面左上が横0・縦0で、右と下が正です。部品の横位置・縦位置は回転前の左上です。方向0度は右、90度は下。1歩と座標の1は1ピクセルで、ペンの太さもピクセル単位です。距離は中心間の距離です。接触判定は回転と拡大を含む外接長方形で、透明な画素の形そのものではありません。動き・ペンは部品で使い、衣装・クローンはスプライトだけで使います。ペンは部品の中心を通ります。「描いた線とスタンプを消す」は画面全体の描画を消します。色は「赤」などの色名または「#ff0000」の形式です。

```text
10歩動く
横100、縦50の位置へ行く
1秒で横200、縦100の位置へ滑る
右に15度回る
左に15度回る
方向を90度にする
横位置を100にする
縦位置を50にする
「マウス」の方向を向く
端に触れていれば跳ね返る
大きさを120％にする
表示色を「赤」にする
隠れる
現れる
前面へ出す
背面へ下げる
1層前へ出す
1層後ろへ下げる
ペンを下ろす
ペンの色を「青」にする
ペンの太さを3にする
ペンを上げる
スタンプを押す
描いた線とスタンプを消す
```

## 画像・音声（素材）・文字・入力欄

「画像・音声を管理」で背景・スプライトの衣装・音を追加します。画像を新しいキャラクターとして追加する「画像を置く」と、音を直接追加する「音声を追加」も使えます。画像はPNG/JPEG/WebP、音はMP3/WAV。画像は元のPNG/JPEG/WebP形式で再エンコードし、素材を作品内に埋め込みます。文字の変更は文字・ボタン・四角に使い、入力欄には文字を入れます。画像の表示色を変更しても画像自体の色は変わりません。名前で指定する部品は元の部品を指します。

```text
背景を「空色」にする
次の背景にする
前の背景にする
衣装を「星」にする
次の衣装にする
前の衣装にする
「ボタン1」の文字を「始める」にする
※ 入力欄を作ってから使う例：
※ 「入力欄1」に「こんにちは」を入れる
※ 「入力欄1」の値と言う
```

## 音と待機

「鳴らす」は音を開始して次の命令へ進み、「鳴らし、終わるまで待つ」はその再生が終わるまで現在の処理だけを待たせます。ほかの音の終了は待ちません。「すべての音を止める」で音待ちも解除します。音量は0〜100％、音程は1段階が半音です。一時停止では論理時計と音声を一時停止します。音声装置の切替は非同期で行われます。

```text
440Hzの音を0.2秒鳴らす
440Hzの音を0.2秒鳴らし、終わるまで待つ
音量を50％にする
音程を1段階上げる
音程を1段階下げる
すべての音を止める
※ 「ベル」という音を追加した場合：
※ 「ベル」を鳴らし、終わるまで待つ
```

## 知らせ・質問・できごと

「知らせる」は画面と全個体の「知らせを受けたとき」を起動します。受け取った知らせを条件で選びます。「受け手の処理が終わるまで待つ」は、その通知で直接起動した各処理の終了を待ちます。受け手が別の通知を待たずに出した場合、その先の処理までは待ちません。自分にも通知が届くので、無条件の再通知は避けます。質問は一つずつ表示され、「答え」は質問した処理ごとに保持されます。

```text
「開始」と知らせる。
「開始」と知らせ、受け手の処理が終わるまで待つ。
「名前は？」とたずねる。
答えと言う。
※ 「知らせを受けたとき」の本文：
※ もし 受け取った知らせが「開始」と同じなら、次のことをする。
※   「受け取りました」と言う
```

## 動作と求値を作る

「動作・計算」で「新しい動作」または「新しい計算」を選び、種類・名前・受け取る値を登録し、本文欄には本文だけを書きます。動作は処理をまとめたもので、呼び出し元の個体で実行します。求値は値を計算して返すもので、外部データの変更・描画・待機はできません。求値の最後には「〜を返す」を一つ置きます。局所変数・リストは本文の先頭で作ります。引数は値渡しで、リストも複製されます。イベント固有の値は引数で渡してください。

```text
※ 動作「ジャンプ」、引数「高さ」の本文例
縦位置を縦位置－高さにする。
0.2秒待つ。
縦位置を縦位置＋高さにする。
※ 呼び出し：ジャンプ（30）を実行する

※ 求値「合計値」、引数「数一覧」の本文例
合計という変数を作り、初期値を0にする。
数一覧の各要素を項目として、次のことをくり返す。
  合計に項目を足す。
合計を返す。
※ 呼び出し：合計値（［1、2、3］）と言う
```

定義の「種類・名前・受け取る値・本文」は、「保存」を押すまで編集中の下書きです。ブロックで本文を変えても、既に登録した定義を黙って更新しません。名前・受け取る値と本文を確認して「保存」すると、定義全体を作品に登録します。未確定の本文入力があるときは、名前・受け取る値・種類の変更も確定または取消を待ちます。登録せず閉じる・別の定義を選ぶ操作では、下書きを破棄する確認を行います。未登録の下書きも共通の履歴で戻せ、自動保存から復元して編集を続けられます。

## クローン・停止・デバッグ

クローンはスプライトの実行時の複製です。個体データと見た目を複製し、作品データは共有します。「クローンされたとき」だけを新しく起動します。実行時の変化やクローンは作品の保存内容に反映しません。「一時停止」「続ける」「1命令ずつ」で調べられます。1命令ずつでも待ち時間は早送りされません。「停止」は実行状態を破棄して編集に戻ります。「最初に戻す」は現在の編集セッションの初期状態へ戻す操作です。

```text
自分のクローンを作る
「星」のクローンを作る
このクローンを削除する
このスクリプトを止める
ほかのスクリプトを止める
すべてを止める
```

実行準備中・取込み中と、実行中・一時停止中は本文を変更できません。実行中や一時停止中にも「コード」「ブロック」を切り替えて、同じ実行位置を調べられます。実行位置は本文の所有者と行に対応する文へ表示します。動作・求値内で停止・失敗したときは、その定義の本文を表示し、選択中の通常本文へ書き戻しません。表示切替で処理の順序、待ち時間、乱数、実行中の値を初期化しません。準備中・取込み中の表示切替は待機します。

## 保存・復元・書き出し

「保存」は .akari.md を作ります。本文にコード・初期データ・部品・素材一覧を表示し、末尾に復元用データを埋め込みます。Markdown本文は読むためのもので、直接編集する場所ではありません。修正はあかりの中で行い、改めて保存します。本文と復元データが一致しないファイルはF512で拒否し、現在の作品を保ちます。

ブロックで確定した通常本文は、コード表示へ切り替えずに保存・自動保存・診断・実行・書き出しできます。「動作・計算」で編集中の下書きは、先にその画面の「保存」で定義全体を作品へ登録します。文法エラーや名前・対象の誤りがある作りかけの本文も、作品の構造・素材・資源上限を満たせばファイル保存できます。開き直すと本文をそのまま復元して診断し、文法エラーの本文はコード表示で開きます。実行・単体HTMLへの書き出しには、作品全体の文法・参照の検査への合格が必要です。

保存するプログラムは日本語の本文です。ブロック用の別プログラムや画面座標を保存しません。保存後に開き直して「ブロック」を選ぶと、本文から同じ命令・式・コメントの構造を再構成します。ブロックの一時選択、展開状態、ドラッグ途中の状態は作品の実行内容に含めません。

### 1.0.0の形式と検証

製品版は1.0.0です。`appVersion`、`languageVersion`、`runtimeVersion`は1.0.0、保存の`formatVersion`と実行データの`projectFormatVersion`・`programFormatVersion`は3です。製品版はリリース、形式番号は保存・実行データの構造契約を識別します。同じ数値でも保存形式と実行形式は別の契約です。

作品ファイルは`akari-project`の復元データを`<!-- AKARI-PROJECT-F3-DATA-BEGIN -->`と`<!-- AKARI-PROJECT-F3-DATA-END -->`で囲みます。閲覧本文の見出しは「あかり 1.0.0 の作品」です。実行データは`akari-executable`を使います。各版・形式は共通定義から生成し、読み込みではこの契約との一致を検証します。編集用の役割範囲・ブロック・選択は永続化せず、本文から再構成します。実行上の診断に必要な行・列だけは生成HTMLにも保持します。

自動保存はIndexedDBの`akari-workspace-f3`、利用できない場合の素材なし保存はlocalStorageの`akari.autosave.f3`を使います。記録内の作品にも同じ製品版・保存形式を適用し、素材、未登録定義の下書き、本文上限を検証してから復元します。UI表示設定の`akari.uiLevel.v1`は作品・自動保存形式とは独立です。

不正・破損データ、本文と復元データの不一致、素材不整合、上限超過の検証と、失敗時に現在作品・未保存内容を守る義務は維持します。入力本文は1.0.0の文法で解析します。

「書き出し」は編集機能を持たない単体HTMLを作ります。コード表示・ブロック表示のどちらからでも同じ確定済み本文を検査・実行形式に変換し、同じ実行処理と素材を含めます。単体HTMLの実行にブロックエディタや外部ファイルは必要ありません。

自動保存はこの端末・ブラウザー内だけです。動作・求値の未登録の下書きも自動保存に含め、復元後は編集を続けられます。利用できない場合や容量不足は表示します。自動保存と別にファイル保存してください。1.0.0の正規仕様に従って記録・検証・復元し、未登録の下書きと確定定義を区別します。外部通信・外部スクリプトの読込みは行いません。

## ライセンスと配布

あかり本体と、単体HTMLへ組み込まれるあかりの実行コードは **Apache License 2.0** のもとで提供されます。ライセンス条件の正本はリポジトリの [`LICENSE`](LICENSE) です。

`Akari.html`、またはあかりの実行コードを含む書き出し済み単体HTMLを再配布する場合は、Apache License 2.0 の条件に従い、受け手へライセンスの写しを提供し、保持が必要な著作権・ライセンス等の表示を残してください。変更を加えたファイルを配布する場合は、Apache License 2.0 が求める変更表示も行ってください。

作品に書いたコード、文章、画像、音など利用者自身の内容について、この文書は一律のライセンスを指定しません。ただし、単体HTMLにはあかりの実行コードが含まれるため、その部分の再配布には Apache License 2.0 の条件が適用されます。

ライセンス条件は編集モードにかかわらず同じです。保存・実行データの整合性は、前節の1.0.0の契約に従います。

## 画面幅と表示縮尺

狭い画面では道具箱、作品の画面、「作品」「せってい」「動きのようす」、コードまたはブロックの本文欄を縦に再配置します。主要操作はツールバー内で折り返します。表示「かんたん」で隠れる機能は「くわしく」または「全部」へ切り替えると利用できます。ダイアログの本文はスクロールできます。ブロックの道具箱と本文も狭い画面では縦に並びます。長い本文・多数の入力はページ、深い入れ子は本文内のスクロールで全体へ到達できます。表示幅や拡大率だけを変えても本文や履歴を変更しません。

画面デザイナは、作品に保存した論理座標を保ったまま表示だけ縮小します。ポインター位置・ドラッグ・配置を論理座標へ換算するため、画面幅だけを変えても部品の位置や大きさは変わりません。未保存コード、カーソル、選択中の部品、実行・一時停止状態も保持します。書き出した単体HTMLでも同じ縮尺処理を使います。

## エラーと上限

「作品チェック」は場所・行・理由を表示します。文法・名前・対象の誤りは実行前、値の型や範囲の誤りは必要に応じて実行時に検出します。実行エラーでは一時停止し、停止して修正できます。Fはファイル、N/Pは文字と文法、Sは参照・意味、Rは実行、Xは書き出し形式のエラーです。無限ループは実行を分割するため停止できますが、求値は計算量の上限で終了します。自己検査のPASSは定義された自動検査への合格であり、すべての環境で未知の不具合がない保証ではありません。

## 入力候補で文法を探す

検索欄に語を入れ、前・次で候補のページを切り替えます。対象外の候補も表示すると、使えない理由を確認できます。命令は空の行または行全体の選択範囲に挿入し、式はカーソル位置または選択範囲に入れます。数値や文字は例なので、目的に合わせて直します。引数の型は呼出し側で決まるため、リスト操作の候補があってもリストを渡す必要があります。引数・局所値・反復の項目は挿入位置に合わせて表示します。局所宣言は本文先頭、返却は求値の末尾、反復の中断は反復内で使います。

## 失敗したときの値を調べる

実行エラーでは一時停止し、失敗記録の画面を開きます。失敗した動作・求値自身の行・命令・呼出し順・引数・局所値・作品・個体データを記録します。「全文」で長い値をページごとに、リストを要素番号ごとに読めます。続行後に現在値が変わっても失敗時の記録は変わりません。実行モニターから記録を開き直せます。保持は直近20件以内かつ記録のJSON合計8,388,608文字を目安に、古い記録から消します。最新1件はこの長さを超えても保持します。「停止」と次の実行で記録は消えます。失敗した処理は終了するため、修正後は改めて実行します。

## キーボード操作と準備中の制限

Tabで操作対象を移動します。作品の一覧は矢印・Home・Endで選び、Enterで開きます。実行開始後は実行画面にフォーカスが移ります。クリック処理のある部品やボタンはTabで選び、Enterまたは空白キーで押せます。質問のEnterは変換中には答えを送信しません。通常のダイアログはEscapeで閉じ、開いた元に戻ります。実行中・素材準備中・取込み中には作品の編集をロックします。素材の準備中は停止できますが、作品や素材の取込み中は終了まで編集操作を待ちます。画像・音の準備の応答が10秒ない場合はエラーにします。

## 読み取れる状態の一覧

`マウスが押されている` ／ `マウスの横位置` ／ `マウスの縦位置` ／ `以前の背景名` ／ `新しい背景名` ／ `受け取った知らせ` ／ `押されたキー` ／ `クローンである` ／ `背景番号` ／ `背景名` ／ `衣装番号` ／ `衣装名` ／ `横位置` ／ `縦位置` ／ `方向` ／ `大きさ` ／ `タイマー` ／ `答え` ／ `音量` ／ `音程` ／ `以前の値` ／ `新しい値`

イベント専用の値：押されたキーはキーを押したとき、受け取った知らせは知らせを受けたとき、以前の値・新しい値は入力欄の値が変わったとき、以前の背景名・新しい背景名は背景が変わったときに使います。キー名は 空白、上、下、左、右、決定、取消、a などです。色の接触は設定色と外接長方形の判定で、画像の画素やペンの描画は調べません。

## 組み込み求値の一覧

- `乱数`（引数2個）、`余り`（引数2個）
- `四捨五入`、`切り上げ`、`切り捨て`、`絶対値`、`平方根`、`正弦`、`余弦`、`正接`、`逆正弦`、`逆余弦`、`逆正接`、`自然対数`、`常用対数`、`指数`、`数`、`文字`（各引数1個）
- `最小`、`最大`（引数1個以上）
- `つなぐ`（引数2個以上）

三角関数の角度は度です。「最初にある番号」は見つからなければ0を返します。四捨五入の負の中間値は0から遠ざかる方向です。

## 命令の標準形一覧

以下は、あかり1.0.0の命令の標準形です。名前・数値・素材名・部品名は例です。ブロック開始文は、本文を字下げして続けます。「そうでなければ、次のことをする。」は直前の同じ字下げ位置の条件分岐に対応する標準形です。

```text
点数を0にする。
点数に1を足す。
点数から1を引く。
名前一覧に「あかり」を追加する。
名前一覧の1番目を「ひかり」にする。
名前一覧の1番目に「ほたる」を挿入する。
名前一覧の1番目を削除する。
名前一覧を空にする。

もし 点数が10以上なら、次のことをする。
  何もしない。
そうでなければ、次のことをする。
  何もしない。
「10点以上です」と言う。

次のことを10回くり返す。
  何もしない。
点数が100より小さいあいだ、次のことをくり返す。
  何もしない。
空白キーが押されるまで、次のことをくり返す。
  何もしない。
0.1秒待つ。
点数が100以上になるまで待つ。
名前一覧の各要素を項目として、次のことをくり返す。
  何もしない。

10歩動く。
横100、縦50の位置へ行く。
1秒で横200、縦100の位置へ滑る。
右に15度回る。
左に15度回る。
方向を90度にする。
横位置を100にする。
縦位置を50にする。
「マウス」の方向を向く。
端に触れていれば跳ね返る。
大きさを120％にする。
表示色を「赤」にする。
隠れる。
現れる。
前面へ出す。
背面へ下げる。
1層前へ出す。
1層後ろへ下げる。
ペンを下ろす。
ペンの色を「青」にする。
ペンの太さを3にする。
ペンを上げる。
スタンプを押す。
描いた線とスタンプを消す。
背景を「空色」にする。
次の背景にする。
前の背景にする。
衣装を「星」にする。
次の衣装にする。
前の衣装にする。
「ボタン1」の文字を「始める」にする。

440Hzの音を0.2秒鳴らす。
440Hzの音を0.2秒鳴らし、終わるまで待つ。
音量を50％にする。
音程を1段階上げる。
音程を1段階下げる。
すべての音を止める。
「ベル」を鳴らす。
「ベル」を鳴らし、終わるまで待つ。

「開始」と知らせる。
「開始」と知らせ、受け手の処理が終わるまで待つ。
「名前は？」とたずねる。

合計という変数を作り、初期値を0にする。
結果というリストを作る。
合計を返す。
増やす（3）を実行する。

自分のクローンを作る。
「星」のクローンを作る。
このクローンを削除する。
このスクリプトを止める。
ほかのスクリプトを止める。
すべてを止める。

文字を「文字」にする。
「入力欄1」に「答え」を入れる。
このくり返しを終える。
次のくり返しへ進む。
次のことをずっとくり返す。
  何もしない。
何もしない。
```

## 資源の上限

継承する `LIMITS` は次のとおりです。これらは保存・素材・実行・言語処理で共通して使う上限です。

| キー | 上限 |
| --- | ---: |
| `fileBytes` | 25165824 |
| `importFileBytes` | 12582912 |
| `assetRawTotal` | 16777216 |
| `assets` | 128 |
| `imageAssets` | 64 |
| `audioAssets` | 64 |
| `imageBytes` | 4194304 |
| `audioBytes` | 6291456 |
| `imageWidth` | 4096 |
| `imageHeight` | 4096 |
| `imagePixels` | 4194304 |
| `imagePixelsTotal` | 16777216 |
| `audioDuration` | 60 |
| `audioDurationTotal` | 120 |
| `audioChannels` | 2 |
| `audioSampleRateMin` | 8000 |
| `audioSampleRateMax` | 48000 |
| `activeAudioVoices` | 32 |
| `backdrops` | 256 |
| `costumesPerSprite` | 256 |
| `sounds` | 256 |
| `components` | 500 |
| `clones` | 500 |
| `scripts` | 1000 |
| `sourceEach` | 100000 |
| `sourceTotal` | 1000000 |
| `projectVars` | 1000 |
| `projectLists` | 500 |
| `listItems` | 100000 |
| `stringChars` | 100000 |
| `callables` | 500 |
| `args` | 32 |
| `callDepth` | 64 |
| `functionOps` | 100000 |
| `blockDepth` | 128 |
| `expressionDepth` | 128 |
| `repeatCount` | 1000000 |
| `activeTasks` | 2000 |

## 本体に残る機械可読情報

入力候補、整形、作品チェック、実行前検査に必要な機械可読の文法・命令・状態・上限情報は `Akari.html` に保持します。この文書は利用者が読む詳細仕様の正本です。

本体とこの文書が食い違う場合は仕様不整合として扱い、意図された仕様と実装を確認して整合させます。

## 関連文書

- あかりの公開概要・導線: public `SAIEduLab/Akari` の `README.md`
- リリース時の検査基準: `AUDIT.md`
